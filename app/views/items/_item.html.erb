<%= image_tag item.picture.url if item.picture? %>
<%= item.name %>
<%= 'price:'%>
<%=  number_to_currency(item.price) %>

<%= button_to 'в корзину', line_items_path(item_id: item) %>
<%= will_paginate @items %>
<p>
